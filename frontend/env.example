# Copier vers .env.local (Create React App)
# URL du FastAPI (doit correspondre à uvicorn, ex. port 8001)
REACT_APP_BACKEND_URL=http://127.0.0.1:8001

# Même secret que DEXTERIO_API_KEY dans backend/.env (si défini côté serveur)
# REACT_APP_DEXTERIO_API_KEY=
